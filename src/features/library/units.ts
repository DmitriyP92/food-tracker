/**
 * Единицы измерения. PRD §10.1 (открытый вопрос PO) — пока фиксированный
 * список; пользовательские единицы добавим, если PO решит иначе.
 */
export const UNITS = ['г', 'мл', 'шт'] as const

export const DEFAULT_UNIT = UNITS[0]
